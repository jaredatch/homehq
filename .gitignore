# Dependencies
node_modules/

# Next.js
.next/
out/

# Build
dist/
/build

# Environment (keep .env.example)
.env
.env.local
.env.*.local

# Runtime data
data/config.json
data/homehq.db
data/homehq.db-wal
data/homehq.db-shm
data/deploy-version
# Local backups of the above (DB backups carry the OAuth token — never commit)
data/*.bak-*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Codex CLI
.codex/
.codexignore

# Claude Code (local-only settings)
.claude/settings.local.json

# Private (maintainer-only: live ops docs, deploy target, household punch list — never committed)
private/

# Debug
npm-debug.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Testing
/coverage
