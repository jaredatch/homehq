GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
COOKIE_SECRET=
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# Optional: Todoist personal API token, for the to-do column on a personal board.
# Todoist -> Settings -> Integrations -> Developer. Leave blank to disable to-dos.
TODOIST_API_KEY=
# Optional, dev only: extra hosts allowed to reach `next dev` (comma-separated)
# HOMEHQ_DEV_ORIGINS=my-mac.tailnet.ts.net,192.168.1.20
